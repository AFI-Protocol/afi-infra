
/**
 * 🧩 Infrastructure Config
 * Central config values for polling, retries, and daemon orchestration.
 */

export const InfraConfig = {
  signalPollingIntervalMs: 3000,
  maxRetryAttempts: 5,
  retryDelayMs: 1000,
};
